# Phase 7 Real E2E configuration (NO API keys - revision #4)
# The real wiring uses the EXISTING runtime implementations:
#   claude -> Claude Code CLI (uses your existing claude auth/config)
#   dsh    -> DSH Desktop v2 bridge (local Desktop endpoint discovery)
#   codex  -> Codex CLI / app-server (uses your existing codex auth/config)
#
# Copy to .env.real_e2e and source it, or export directly.

# Real mode (fail-closed: refuses to fall back to fake)
RUNTIME_MODE=real

# Timeout for the whole E2E run
E2E_TIMEOUT_SECONDS=300
